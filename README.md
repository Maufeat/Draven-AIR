# Draven-AIR
[7.12] RTMPS Server Emulator for League of Legends AIR Client


#Tutorial
You need to create a mysql database with the name "lol"(can be configured in the program.cs), and import the two .sql files inside the Database folder.

TODO make this more gut
